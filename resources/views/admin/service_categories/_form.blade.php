<div class="row g-3"><div class="col-xl-8"><x-card type="primary" title="Thông tin danh mục"><x-input name="name" label="Tên danh mục" :value="$category->name" required /><x-input name="slug" label="Slug" :value="$category->slug" required /><x-input name="kicker" label="Kicker" :value="$category->kicker" /><x-textarea name="description" label="Mô tả" :value="$category->description" rows="7" /></x-card></div><div class="col-xl-4"><x-card type="info" title="Hiển thị"><x-input name="icon" label="Bootstrap icon" :value="$category->icon" placeholder="bi-grid" /><x-input name="sort_order" type="number" label="Thứ tự" :value="$category->sort_order ?: 0" /><label class="form-check form-switch"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active', $category->exists ? $category->is_active : true))><span class="form-check-label">Kích hoạt</span></label></x-card></div><div class="col-12 text-end"><a href="{{ route('admin.service-categories.index') }}" class="btn btn-default">Hủy bỏ</a> <button class="btn btn-primary">{{ $submitLabel }}</button></div></div>
