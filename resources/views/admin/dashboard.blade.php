@extends('layouts.admin')
@section('page-title', 'Tổng quan')
@section('content')
<div class="row g-3 mb-4">@foreach([['Tour đang có',$totalTours,'bi-map','primary'],['Điểm đến',$totalDestinations,'bi-geo-alt','success'],['Tổng booking',$totalBookings,'bi-calendar-check','info'],['Booking chờ xử lý',$pendingBookings,'bi-hourglass-split','warning']] as [$label,$value,$icon,$color])<div class="col-md-6 col-xl-3"><div class="card h-100"><div class="card-body d-flex justify-content-between"><div><div class="text-muted">{{ $label }}</div><div class="fs-3 fw-bold">{{ $value }}</div></div><i class="bi {{ $icon }} fs-2 text-{{ $color }}"></i></div></div></div>@endforeach</div>
<div class="card"><div class="card-header"><h3 class="card-title">Booking gần đây</h3><a href="{{ route('admin.bookings.index') }}" class="btn btn-sm btn-outline-primary float-end">Xem tất cả</a></div><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Mã booking</th><th>Khách hàng</th><th>Tour</th><th>Trạng thái</th><th class="text-end">Tổng tiền</th></tr></thead><tbody>@forelse($recentBookings as $booking)<tr><td class="fw-semibold">{{ $booking->booking_code }}</td><td>{{ $booking->customer_name }}<small class="d-block text-muted">{{ $booking->customer_phone }}</small></td><td>{{ $booking->items->first()?->tour_name ?? '—' }}</td><td><span class="badge text-bg-light">{{ $booking->status }}</span></td><td class="text-end">{{ number_format($booking->total_amount, 0, ',', '.') }} ₫</td></tr>@empty<tr><td colspan="5" class="text-center py-5">Chưa có booking.</td></tr>@endforelse</tbody></table></div></div>
@endsection
