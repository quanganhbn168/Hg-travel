@extends('layouts.admin')
@section('title','Chỉnh sửa danh mục bài viết')
@section('page-title','Chỉnh sửa danh mục bài viết')
@section('content')
<form action="{{ route('admin.post-categories.update',$category) }}" method="POST">@csrf @method('PUT')<div class="row g-3"><div class="col-xl-8"><x-card type="primary" title="Thông tin"><x-input name="name" label="Tên danh mục" :value="$category->name" required/><x-input name="slug" label="Slug" :value="$category->slug"/><x-textarea name="description" label="Mô tả" :value="$category->description" rows="8"/></x-card></div><div class="col-xl-4"><x-card type="info" title="Cấu hình" class="mb-3"><x-select name="parent_id" label="Danh mục cha" :options="$parents->pluck('name','id')->all()" :selected="$category->parent_id" placeholder="Không có"/><x-input name="sort_order" type="number" label="Thứ tự" :value="$category->sort_order"/><label class="form-check form-switch"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked(old('is_active',$category->is_active))><span class="form-check-label">Hiển thị</span></label></x-card><x-card type="secondary" title="SEO"><x-input name="seo_title" label="SEO title" :value="$category->seo_title"/><x-textarea name="seo_description" label="SEO description" :value="$category->seo_description" rows="3"/></x-card></div><div class="col-12 text-end"><a class="btn btn-outline-secondary" href="{{ route('admin.post-categories.index') }}">Hủy</a> <button class="btn btn-primary">Lưu thay đổi</button></div></div></form>
@endsection
