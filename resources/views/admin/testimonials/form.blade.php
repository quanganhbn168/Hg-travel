@extends('layouts.admin')
@section('title','Cảm nhận khách hàng') @section('page-title',$testimonial->exists ? 'Sửa cảm nhận' : 'Thêm cảm nhận')
@section('content')<form method="POST" action="{{ $testimonial->exists ? route('admin.testimonials.update',$testimonial) : route('admin.testimonials.store') }}">@csrf @if($testimonial->exists)@method('PUT')@endif<div class="row"><div class="col-lg-8"><x-card title="Nội dung"><x-input name="customer_name" label="Tên khách hàng" :value="$testimonial->customer_name" required/><x-input name="customer_title" label="Chức danh / mô tả" :value="$testimonial->customer_title"/><x-textarea name="content" label="Cảm nhận" :value="$testimonial->content" rows="7" required/></x-card></div><div class="col-lg-4"><x-card title="Hiển thị"><x-image-upload name="avatar_path" label="Ảnh đại diện" :value="$testimonial->avatar_path"/><x-input type="number" name="rating" label="Đánh giá (1-5)" :value="$testimonial->rating" required/><x-input type="number" name="sort_order" label="Thứ tự" :value="$testimonial->sort_order"/><label class="form-check form-switch"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked($testimonial->is_active)><span class="form-check-label">Hiển thị</span></label></x-card></div><div class="col-12 text-end mt-3"><button class="btn btn-primary">Lưu cảm nhận</button></div></div></form>@endsection
