@extends('layouts.admin')
@section('title','Menu website') @section('page-title',$menu->exists ? 'Sửa menu' : 'Thêm menu')
@section('content')<form method="POST" action="{{ $menu->exists ? route('admin.menus.update',$menu) : route('admin.menus.store') }}">@csrf @if($menu->exists)@method('PUT')@endif<div class="row"><div class="col-lg-8"><x-card title="Thông tin menu"><x-input name="name" label="Tên menu" :value="$menu->name" required/><x-input name="location" label="Vị trí (header hoặc footer)" :value="$menu->location" required/><label class="form-check form-switch"><input class="form-check-input" type="checkbox" name="is_active" value="1" @checked($menu->is_active)><span class="form-check-label">Hiển thị</span></label></x-card></div><div class="col-lg-8 text-end mt-3"><button class="btn btn-primary">Lưu menu</button></div></div></form>
@if($menu->exists)<hr class="my-4"><x-card title="Mục menu"><div class="table-responsive"><table class="table"><thead><tr><th>Tiêu đề</th><th>Liên kết</th><th>Cha</th><th>TT</th><th></th></tr></thead><tbody>@forelse($items as $item)<tr><td>{{ $item->title }}</td><td>{{ $item->route_name ?: $item->url }}</td><td>{{ $item->parent?->title ?: '—' }}</td><td>{{ $item->position }}</td><td class="text-end"><form method="POST" action="{{ route('admin.menus.items.destroy',[$menu,$item]) }}">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Xóa</button></form></td></tr>@empty<tr><td colspan="5" class="text-center">Chưa có mục menu.</td></tr>@endforelse</tbody></table></div></x-card><x-card title="Thêm mục menu" class="mt-3"><form method="POST" action="{{ route('admin.menus.items.store',$menu) }}">@csrf<div class="row"><div class="col-md-6"><x-input name="title" label="Tiêu đề" required/><x-input name="url" label="URL"/><x-input name="route_name" label="Tên route Laravel (nếu dùng)"/></div><div class="col-md-6"><x-select name="parent_id" label="Mục cha" :options="$items->pluck('title','id')->all()" placeholder="Không có"/><x-select name="target" label="Cách mở" :options="['_self'=>'Cùng tab','_blank'=>'Tab mới']" selected="_self"/><x-input type="number" name="position" label="Thứ tự" value="0"/><label class="form-check form-switch"><input class="form-check-input" type="checkbox" name="is_active" value="1" checked><span class="form-check-label">Hiển thị</span></label></div></div><div class="text-end"><button class="btn btn-primary">Thêm mục</button></div></form></x-card>@endif@endsection
